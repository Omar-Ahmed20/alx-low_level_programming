#include "main.h"

/**
 * mul - return the multiplicarion of two ints
 * @a: first input
 * @b: second user input
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
